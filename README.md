# firebase-admin-cloudflare

Firestore Admin SDK implementation for Cloudflare Workers (REST + WebChannel).

Status: early / work-in-progress.

## Dev

- `npm run check` runs formatting, lint, tests, and build.
- Example Worker: see `examples/README.md`.
